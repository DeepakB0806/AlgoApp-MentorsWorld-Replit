Task #85: Enterprise Trade Engine Loophole Patch (Final Revision)Objective:
Apply 4 critical security patches to server/te-kotak-neo-v3.ts using the Enterprise State Machine architecture. We must ensure that margin rollbacks survive server reboots and Kotak API crashes by using the database states created in Task #84.1. Fix 1: Minimum Lot Size Circuit Breaker (CRITICAL)In executeLegBasket, prevent "brokerage bleeding" when the lotMultiplier cannot be reduced further.Logic: Inside the margin shortfall block, check if effectiveMultiplier <= 1.Action: If true, log a [ABORT] PFL error and return (do not just break) to stop all further retry attempts for that signal.2. Fix 2: Partial Fill Quantity Persistence (CRITICAL)Ensure the database reflects actual holdings, not just intended quantity.Logic: Use getFillPrice to capture filledQty.Action: Define actualFilledQty. If Kotak reports a partial fill, update actualFilledQty and use this value for both the landedLegs array and the storage.createStrategyTrade call.3. Fix 3: Enterprise Rollback State Machine (HIGH)Refactor the rollback logic to be database-persistent instead of an inline RAM loop.Immediate Write: Inside executeLegBasket, as soon as an order succeeds, immediately run storage.createStrategyTrade with status: "pending_basket". This ensures every leg has a database ID before a rollback starts.State Updates: * Before rollback: Update trade to status: "rolling_back".If rollback succeeds: Update to status: "rolled_back".If rollback FAILS (API Crash): Update to status: "rollback_failed".Background Loop: Replace the inline for loop with a new function startPersistentRollback(storage, planId, brokerConfig). This function must:Query for trades with status: "rollback_failed".Check against the rollback_api_retry_count setting.Persistently retry the reversal until success or retries are exhausted (then log [FATAL]).4. Fix 4: Boot-Up Recovery & Manual Exit Detection (HIGH)4a (Manual Exit): In closeTrade, if Kotak returns an "Insufficient Holdings" error, mark the trade as status: "closed" (Manually Squared) to prevent infinite exit loops.4b (Boot-up Catcher): In server/index.ts, update the recovery logic to query for both close_failed and rollback_failed trades. Re-ignite the appropriate persistent loop (startPersistentExit or startPersistentRollback) for each on startup.Constraint: * No new schema changes (use the pending_basket, rolling_back, rolled_back, and rollback_failed states already added in Task #84).End of copy-pasteWhy this version wins:Perfect Audit Trail: Every time your lot size steps down, the database will show exactly which legs were rolled_back and why.Crash Proof: If your internet drops or the server reboots during a rollback, the "Boot-up Catcher" will see the rollback_failed trades and resume the reversal automatically.Capital Protection: You won't pay for 5 repeated 1x lot size rollbacks if your account is simply out of funds.